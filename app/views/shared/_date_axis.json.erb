{
    title: '<%= local_assigns[:title] || t('widgets.titles.date') %>',
        format: 'dd/M/yy',
    gridlines: {
    units: {
        years: {format: ['yyyy']},
        months: {format: ['M/yy']},
        days: {format: ['dd']},
    },
},
    minorGridlines: {
        units: {
            years: {format: ['yyyy']},
            months: {format: ['MM']},
            days: {format: ['dd']},
        },
        color: 'grey',
    }
}