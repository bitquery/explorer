<% content_for :title, t( "tabs.#{controller_name}.#{params[:action]}.title", network: @network[:name], hash: @hash) %>
<% content_for :description, t( "tabs.#{controller_name}.#{params[:action]}.description", network: @network[:name], hash: @hash) %>

<%= render partial: 'shared/title', locals: {
          icon: t("tabs.#{controller_name}.icon"),
          header: t( "tabs.#{controller_name}.header",
                     network: @network[:name], hash: copy_text(@hash, 'text-info')).html_safe,
          reportrange: false} %>
