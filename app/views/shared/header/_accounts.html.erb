<% content_for :title, t("tabs.#{controller_name}.#{params[:action]}.title", network: @network[:name], account: @account_id) %>

<% content_for :description, t("tabs.#{controller_name}.#{params[:action]}.description", network: @network[:name], account: @account_id) %>

<% content_for :keywords, t("tabs.#{controller_name}.#{params[:action]}.keywords", network: @network[:name], account: @account_id) %>

<%= render partial: 'shared/title', locals: {
          icon: t("tabs.#{controller_name}.icon"),
          header: t("tabs.#{controller_name}.header",
                     network: @network[:name], account: copy_text(@account_id, 'text-info')).html_safe,
          reportrange: false} %>
