<% content_for :title, t( "tabs.#{controller_name}.#{params[:action]}.title", network: @network[:name], order: @order) %>
<% content_for :description, t( "tabs.#{controller_name}.#{params[:action]}.description", network: @network[:name], order: @order) %>

<%= render partial: 'shared/title', locals: {
          icon: t("tabs.#{controller_name}.icon"),
          header: t( "tabs.#{controller_name}.header",
                     network: @network[:name], order: copy_text(@order, 'text-info')).html_safe,
          reportrange: false} %>
