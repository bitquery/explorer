<% content_for :title, t( "pages.tx_list.#{params[:action]}.title", network: @network[:name]) %>
<% content_for :description,t( "pages.tx_list.#{params[:action]}.description", network: @network[:name]) %>
<% content_for :keywords,t( "pages.tx_list.#{params[:action]}.keywords", network: @network[:name]) %>

<%= render partial: 'shared/title', locals: {
        icon: t("pages.tx_list.icon"),
        header: t( "pages.tx_list.#{params[:action]}.header", network: @network[:name]),
        reportrange: true} %>

<%= render partial: 'shared/filters' %>