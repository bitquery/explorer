<script>
    widgets.callbacks({
        block_path: function(item){
            return '<%= @network[:network] %>'+'/block/'+item.height;
        },
        validator_path: function(item){
            return '<%= @network[:network] %>'+'/validator/'+item.validator.index;
        },
        proposer_path: function(item){
            return '<%= @network[:network] %>'+'/validator/'+item.proposer.index;
        },
        validator_path: function(item){
            return '<%= @network[:network] %>'+'/validator/'+item.validator.index;
        },
        slashing_proposer_path: function(item){
            return '<%= @network[:network] %>'+'/validator/'+item.slashing.proposer.index;
        }
    });

</script>