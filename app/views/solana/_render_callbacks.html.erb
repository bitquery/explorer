<script>
  widgets.callbacks({
    block_path: function(item){
      return '<%= @network[:network] %>'+'/block/'+ item.height;
    },
    block_height_path: function(item){
      return '<%= @network[:network] %>'+'/block/'+item.block.height;
    },
    tx_path: function(item){
      return '<%= @network[:network] %>'+'/tx/'+ (item.signature || item.transaction.signature);
    },
    address_path: function(item){
      if (item.sender && item.sender.address) {
        return '<%= @network[:network] %>'+'/address/'+item.sender.address;
      }

      if (item.receiver && item.receiver.address) {
        return '<%= @network[:network] %>'+'/address/'+item.receiver.address;
      }

      if (item.account) {
        return '<%= @network[:network] %>'+'/address/'+item.account;
      }

      if (item.program.id){
        return '<%= @network[:network] %>'+'/address/'+item.program.id;
      }
    },
    signature_path: function(item){
        return '<%= @network[:network] %>'+'/tx/'+ (item.signature || item.transaction.signature);
    },
  });
</script>
