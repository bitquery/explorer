<%
  nft_tabs = (@streaming && @fungible == false) ? ['nft_address'] : ['show']

%>
<%= render partial: 'shared/tabs', locals: {

    tabs:  @streaming ?
             %w[show multichain nft_address transactions inflow outflow calls_contracts money_flow_lite] :
             %w[show multichain transactions inflow outflow calls_contracts money_flow_lite]
} %>
