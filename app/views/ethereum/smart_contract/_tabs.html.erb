<%= render partial: 'shared/tabs', locals: {
    tabs: 
        (@info.try(:smart_contract).try(:contract_type)=='DEX') ?
          %w[show transactions methods events dex_trades dex_tokens inflow outflow calls_contracts money_flow_lite] :
          %w[show transactions methods events inflow outflow calls_contracts money_flow_lite]
} %>
