<%= render partial: 'shared/tabs', locals: {
    tabs: @streaming ? ['show','tracing','transfers','dex_trades','calls','events','references'] : ['show','transfers','dex_trades','calls','events','references']
} %>
