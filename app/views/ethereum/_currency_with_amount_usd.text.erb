function(item){
  return <%= defined?(currency) ? currency : 'escapeHtml(item.currency.symbol)' %> + '&nbsp;<sup class="text-success">' + ((amount = <%= defined?(amount) ? amount : 'item.amount_usd' %>) > 0 ? '$'+amount.toFixed(<%= defined?(precision) ? precision : 2 %>) : '-') +'</sup>'
}