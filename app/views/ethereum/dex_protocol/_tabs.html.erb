<% if api_version_for_protocol == 'v2' %>
  <% tabs_to_render = ['statistics', 'trades'] %>
<% else %>
  <% tabs_to_render = ['statistics', 'volume', 'trades', 'exchanges', 'pairs', 'traders'] %>
<% end %>

<%= render partial: 'shared/tabs', locals: { tabs: tabs_to_render } %>
