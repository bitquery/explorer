query ($network: AlgorandNetwork){
  chain: algorand(network: $network ){
    transactions(options:{asc: "date.month"}
    ) {
      date{
        month: date(format: "%Y-%m")
      }
      count
    }
  }
}