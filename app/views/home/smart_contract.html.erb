<%= render partial: 'home/chains',locals: {platform: 'Smart Contract', query: '
            query ($network: EthereumNetwork){
                    chain: ethereum(network: $network ){
                      transactions(options:{asc: "date.month"}
                      ) {
                        date{
                          month: date(format: "%Y-%m")
                        }
                        count
                      }
                    }
                  }'} %>