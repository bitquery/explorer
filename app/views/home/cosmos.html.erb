<%= render partial: 'home/chains',locals: {platform: 'Cosmos', query: 'query {
      chain: binance {
        transactions(options:{asc: "date.month"}) {
          date{
            month: date(format: "%Y-%m")
          }
          count
        }
      }
    }'} %>