<%= render partial: 'home/chains',locals: {family: 'bitcoin', query: 'query {
      chain: bitcoin {
        transactions(options:{asc: "date.month"}) {
          date{
            month: date(format: "%Y-%m")
          }
          count
        }
      }
    }'} %>