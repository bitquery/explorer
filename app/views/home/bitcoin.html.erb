<%= render partial: 'home/chains',locals: {platform: 'Bitcoin', query: 'query {
      chain: bitcoin {
        transactions(options:{asc: "date.month"}) {
          date{
            month: date(format: "%Y-%m")
          }
          count
        }
      }
    }'} %>