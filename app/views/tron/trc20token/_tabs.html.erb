<%= render partial: 'shared/tabs', locals: {
    tabs: (@native_token ? ['show','transfers','senders','receivers','trades'] :

             ( (@info.try(:smart_contract).try(:contract_type)=='DEX') ?
                 ['show','transfers','token_dex_trades','dex_trades', 'senders','receivers','smart_contract','transactions',
                  'methods','events','inflow','outflow','calls_contracts','trades'] :
                 ['show','transfers','token_dex_trades', 'senders','receivers','smart_contract','transactions',
                  'methods','events','inflow','outflow','calls_contracts','trades'] )

    )

} %>
