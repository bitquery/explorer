query ($dateFormat: String!, $from: ISO8601DateTime, $till: ISO8601DateTime){
  chain: tron{
    transactions(options:{asc: "date.month"}, date: {since: $from, till: $till }, height: {gt: 0}) {
      date{
        month: date(format: $dateFormat)
      }
      count
    }
  }
}