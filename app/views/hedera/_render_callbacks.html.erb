<script>
  widgets.callbacks({
    transaction_hash_path: function(item){
      return '<%= @network[:network] %>'+'/tx/'+ (item.hash || item.transactionHash);
    },
    topic_path: function(item) {
      return '<%= @network[:network] %>' + '/topics/' + item.topic.id;
    },
    account_path: function(item) {
      return '<%= @network[:network] %>' + '/accounts/' + item.account.id;
    },
    node_path: function(item) {
      return '<%= @network[:network] %>' + '/nodes/' + (item.nodeAccount.id || item.node.id);
    },
    payer_path: function(item) {
      return '<%= @network[:network] %>' + '/payers/' + (item.payerAccount.id || item.payer.id);
    },
    contract_path: function(item) {
      return '<%= @network[:network] %>' + '/contracts/' + item.contract.id;
    },
  });
</script>
