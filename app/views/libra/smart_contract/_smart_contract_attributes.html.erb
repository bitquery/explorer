<div class="col-12 col-lg-6 mb-4">
  <div class="card">
    <div class="card-header"><%= t('widgets.headers.smart_contract_attributes') %></div>
    <div class="card-body">
      <div id="smart_contract_attributes"><%= t('loading') %></div>
    </div>
  </div>
</div>

<script>

    $( document ).ready(function() {
        var query = new widgets.query(`
                        query (
                        $address: String!
                  ){
                  libra{
                      address(address: {is: $address }){
                      smartContract {
                          attributes{
                            name
                            type
                            address {
                              address
                              annotation
                            }
                            value
                          }
                        }
                      }
                    }

                  }`);

        new widgets.table('#smart_contract_attributes', query, 'libra.address', {
            title: '<%= t('widgets.headers.smart_contract_attributes') %>',
            renderData: function(data){
                return data[0].smartContract.attributes;
            },
            dataOptions: [
                {
                    title: '<%= t('widgets.titles.property') %>',
                    type: 'string-ellipsis',
                    path: 'name',
                    html_class: 'text-info font-weight-bold'
                },
                {
                    title:  '<%= t('widgets.titles.property_value') %>',
                    type: 'string-ellipsis',
                    path: 'value',
                    urlCallbackName: 'property_value_path'
                },
            ]
        });

        queryWithTimeRange(rr, query, <%= @from.html_safe %>,<%= @till.html_safe %>,{

            address: '<%= @address %>'
        });


    });

</script>